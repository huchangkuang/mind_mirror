## 1. 测试合集首页 (Test Hub Homepage)

- [x] 1.1 重写 `app/page.tsx` - 创建测试导航首页
  - 复用现有的 Hero 渐变背景和玻璃拟态样式
  - 创建响应式卡片网格布局（移动端1列，平板2列，桌面3列）
  - 包含页面标题、副标题和测试卡片区域

- [x] 1.2 创建测试卡片组件
  - 设计统一的测试卡片：图标 + 标题 + 描述 + 时长 + 开始按钮
  - 实现悬停动画效果（与现有 Button/Card 组件风格一致）
  - MBTI 卡片使用 Brain 图标，城市匹配卡片使用 Building2 图标

- [x] 1.3 实现导航功能
  - 点击 MBTI 卡片跳转到 `/mbti`
  - 点击城市匹配卡片跳转到 `/city-match`

## 2. 性格匹配城市测试 - 数据层

- [x] 2.1 创建 `data/city-match/` 目录

- [x] 2.2 创建 `data/city-match/questions.json`
  - 包含 12 道题目，每题 4 个选项
  - 覆盖 4 个维度：生活方式、社交偏好、环境偏好、节奏偏好
  - 每个选项配置维度权重值

- [x] 2.3 创建 `lib/city-match/types.ts`
  - 定义问题、选项、维度得分、城市匹配结果等类型

- [x] 2.4 创建 `lib/city-match/cities-data.ts`
  - 定义 8-10 个国内外城市的特征档案
  - 每个城市包含：名称、国家、4 维度特征值、描述、图片

## 3. 性格匹配城市测试 - 评分引擎

- [x] 2.5 创建 `lib/city-match/scoring.ts`
  - 实现维度得分计算函数
  - 实现余弦相似度算法匹配城市
  - 返回 TOP 3 匹配城市及匹配度百分比

- [x] 2.6 创建 `lib/city-match/history-storage.ts`
  - 实现 localStorage 历史记录存取
  - 定义历史记录数据结构

## 4. 性格匹配城市测试 - 状态管理

- [x] 2.7 创建 `stores/city-match-store.ts`
  - 使用 Zustand 实现状态管理
  - 包含：当前问题索引、答案记录、维度得分、sessionStorage 持久化
  - 提供开始测试、提交答案、完成测试、重置等方法

## 5. 性格匹配城市测试 - 页面

- [x] 2.8 创建 `app/city-match/page.tsx` - 介绍页
  - 展示测试标题、简介、预计时长（5分钟）
  - 复用现有的介绍页布局模式
  - "开始测试"按钮跳转到 `/city-match/test`

- [x] 2.9 创建 `app/city-match/test/page.tsx` - 测试页
  - 复用 Card + ProgressBar 组件模式
  - 加载并显示题目，支持选项选择
  - 完成所有题目后自动跳转到结果页

- [x] 2.10 创建 `app/city-match/result/page.tsx` - 结果页
  - 展示 TOP 3 匹配城市卡片
  - 每个城市显示：名称、匹配度百分比、简介、图片
  - 维度分布可视化（复用 MBTI 的条形图模式）
  - 提供"重新测试"和"查看历史"按钮

- [x] 2.11 创建 `app/city-match/history/page.tsx` - 历史记录页
  - 展示过往测试结果列表
  - 每条记录显示：日期、匹配城市、重新测试按钮

## 6. MBTI 路由调整

- [x] 3.1 移动现有首页内容到 `app/mbti/page.tsx`
  - 将当前 `app/page.tsx` 内容复制到 `app/mbti/page.tsx`
  - 调整页面内的导航链接（如需要）

- [x] 3.2 验证 MBTI 测试完整流程
  - 确认 `/mbti` 介绍页正常显示
  - 确认 `/mbti/test` 测试流程正常
  - 确认 `/mbti/result` 结果页正常
  - 确认历史记录功能正常

## 7. 集成测试与优化

- [x] 4.1 测试完整用户流程
  - 从首页点击 MBTI 卡片 → 完成 MBTI 测试 → 返回首页
  - 从首页点击城市匹配卡片 → 完成城市测试 → 返回首页
  - 验证城市测试历史记录功能

- [x] 4.2 响应式布局测试
  - 在移动端验证首页卡片布局
  - 验证城市测试结果页的适配

- [x] 4.3 代码清理
  - 移除调试代码
  - 确保无 TypeScript 错误
